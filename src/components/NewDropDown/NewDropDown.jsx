import React, {Component} from 'react';

export default function NewDropDown(){
    printTheYear = (begin, end) => {
        Date newYear = new Date(begin);
    }

    return(
        <select >

        </select>
    );
};